---
import SectionHeader from "../ui/SectionHeader.astro";
import { capabilities } from "../../data/projects";
---

<section class="section" id="systems-map" data-journey-section data-journey-label="Systems map" data-journey-index="5">
  <div class="section-inner">
    <div class="section-marker" aria-hidden="true">
      <span>06</span>
      <strong>Systems Map</strong>
    </div>
    <SectionHeader
      eyebrow="Systems map"
      title="Capabilities tied to evidence"
      body="How the projects connect across AI, product, data, and automation work."
    />
    <div class="capability-grid">
      {capabilities.map((capability) => (
        <article class="lab-entry">
          <h3>{capability.label}</h3>
          <p>{capability.evidence.join(" / ")}</p>
        </article>
      ))}
    </div>
  </div>
</section>
